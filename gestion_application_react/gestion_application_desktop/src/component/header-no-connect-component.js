import React from 'react'
import logo from '../images/logo.png'
const HeaderNoConnect = ({ src, alt, id, value, className }) => (
    <div className='headerNoconnect'>
        <img src={logo} alt={alt} className='imageHeader' />
    </div>

)
export default HeaderNoConnect
